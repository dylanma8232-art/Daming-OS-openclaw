# 白皮书融合验收矩阵

本矩阵以服务器上的 `SYSTEM_NAV.md`、记忆系统 3.0 与成长系统 2.0 白皮书为验收来源。完成标准不是“存在类或脚本”，而是每项可由 Daming OS 的默认 Runtime、Hook 或配置 Provider 驱动，并有自动化测试。

> 注意：脚本名或状态表不是能力边界。以下每一行对应去重后的白皮书能力，并须有“默认触发点 + 自动化测试”才计为接通；任何宿主特有 Provider（如飞书卡片）均由可替换接口承载，不能成为 OpenClaw 依赖。

| 系统 | 白皮书生产能力 | 独立 Daming OS 验收标准 | 状态 |
|---|---|---|---|
| 记忆 | 运行时自动召回/捕获 | `before_turn` 注入召回，`after_turn` 写热记忆 | 已接通 |
| 记忆 | Hot 原子日志与会话折叠 | 文件锁追加、窗口压缩、会话目录隔离 | 已接通 |
| 记忆 | Warm 向量 + FTS | consolidation 写入 LanceDB/SQLite，混合查询命中 | 已接通 |
| 记忆 | Cold Wiki 与关系图谱 | 本地 Wiki、SQLite 关系与默认图扩散 | 已接通 |
| 记忆 | L1/L2 语义缓存 | 精确/向量命中、作用域隔离、写入后失效 | 已接通 |
| 记忆 | 上下文压缩 | Hook 默认交付受限上下文，不依赖宿主 `/compact` | 已接通 |
| 记忆 | 日/周回顾与深睡眠维护 | 固定时刻 Scheduler 运行 consolidation、日/周 review 与同步 | 已接通 |
| 记忆 | 冰川归档与恢复 | 可验证归档、压缩、恢复；作为显式离线操作提供 | 已接通 |
| 记忆 | Skill 懒加载及使用审计 | 按需加载、使用审计账本 | 已接通 |
| 记忆 | Wiki/Bitable 双向同步 | 通用 Provider；本地默认与外部 Provider 注入 | 已接通 |
| 记忆 | 质量门、迁移、健康、基准 | 质量门与健康探针由 Runtime 默认实例化 | 已接通 |
| 成长 | 事件日志与 GEP 衰减 | Hook 错误/发现进入 JSONL，GEP 阈值触发提案 | 已接通 |
| 成长 | 五维主动巡检 | scheduler、经验、规则、路径、安全均有默认探针 | 已接通 |
| 成长 | 事件聚类与经验提炼 | 重复事件形成经验、人工验证后进入技能候选 | 已接通 |
| 成长 | Builder/Reviewer 审计 | 默认 Builder/Reviewer/Judge 三方审计；外部审计器可注入 | 已接通 |
| 成长 | OTP 审批与提醒 | OTP、三次锁定、逾期提醒和本地 outbox；通知 Provider 可替换 | 已接通 |
| 成长 | AST 沙箱、冒烟测试、部署与回滚 | 代码提案默认冒烟测试；技能产物受 Markdown/路径门控；工作流强制验证→OTP→部署→验证/回滚 | 已接通 |
| 成长 | 提案追踪、质量门、版本审计 | 持久化提案/治理账本、质量门、发布账本 | 已接通 |
| 运行时 | Hook、Heartbeat、Cron 替代 | Hook Bridge、持久化 Scheduler、无 Hook 常驻运行模式 | 已接通 |
| 运行时 | 平台边界 | 无 OpenClaw SDK；通知、Wiki、Bitable、部署均为 Provider | 已接通 |

## 不能作为“完成”的情况

- 仅有代码文件但 Runtime、Hook、Scheduler 未调用；
- 只能由 OpenClaw Cron、插件、技能加载或飞书通道触发；
- 仅有 Protocol、无默认本地实现和无验收测试；
- 绕过验证、人工批准或回滚的自动部署。
